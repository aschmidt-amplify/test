name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3  # Ensure a stable version of checkout action is used

      - name: Get version from DESCRIPTION
        id: version
        run: |
          version=$(grep '^Version: ' DESCRIPTION | sed 's/Version: //')
          echo "version=${version}" >> $GITHUB_ENV

      - name: Check if Tag Exists on Remote
        id: check_tag_exists
        run: |
          if git ls-remote --tags origin "v${{ env.version }}" | grep "refs/tags/v${{ env.version }}" >/dev/null; then
            echo "Tag v${{ env.version }} already exists on remote."
            echo "exists=true" >> $GITHUB_ENV
          else
            echo "exists=false" >> $GITHUB_ENV
          fi

      - name: Exit if Tag Exists
        if: env.exists == 'true'
        run: |
          echo "::warning ::The tag v${{ env.version }} already exists. Exiting without creating a new release."
          exit 0

      - name: Tag the current commit
        if: env.exists == 'false'
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git tag -a "v${{ env.version }}" -m "Release v${{ env.version }}"
          git push origin "v${{ env.version }}"

      - name: Create Changelog
        if: env.exists == 'false'
        id: build_changelog
        uses: mikepenz/release-changelog-builder-action@v5
        with:
          mode: "COMMIT"
          configurationJson: |
            {
              "template": "# Changelog for v${{ env.version }}\n\n## Changes\n{{COMMITS}}\n\n## Contributors\n{{CONTRIBUTORS}}",
              "commit": {
                "title": "- {{MESSAGE}} [view commit]({{COMMITS}})",
                "message": "- {{SCOPE}}: {{MESSAGE}} by {{AUTHOR}} [view commit]({{URL}})"
              },
              "contributors": "Thanks to the following contributors: {{CONTRIBUTORS}}"
            }
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Release
        uses: mikepenz/action-gh-release@v0.2.0-a03
        with:
          tag_name: v${{ env.version }}
          body: ${{steps.build_changelog.outputs.changelog}}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
